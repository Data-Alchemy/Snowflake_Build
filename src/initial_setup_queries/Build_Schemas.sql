

USE DATABASE BRONZE_DB;
Create or Replace Schema LEGACY_DW COMMENT        = 'Schema for LEGACY_DW related data';
Create or Replace Schema RETAIL_DATA COMMENT      = 'Schema for RETAIL_DATA related data';
Create or Replace Schema FINANCE_DATA COMMENT     = 'Schema for FINANCE_DATA related data';
Create or Replace Schema APPLICATION_DATA COMMENT = 'Schema for APPLICATION_DATA related data';
Create or Replace Schema MASTER_DATA COMMENT      = 'Schema for MASTER_DATA related data';
Create or Replace Schema PEOPLE_DATA COMMENT      = 'Schema for PEOPLE_DATA related data';
Create or Replace Schema SUPPLY_CHAIN COMMENT     = 'Schema for SUPPLY_CHAIN related data';


USE DATABASE SILVER_DB;
Create or Replace Schema LEGACY_DW COMMENT        = 'Schema for LEGACY_DW related data';
Create or Replace Schema RETAIL_DATA COMMENT      = 'Schema for RETAIL_DATA related data';
Create or Replace Schema FINANCE_DATA COMMENT     = 'Schema for FINANCE_DATA related data';
Create or Replace Schema APPLICATION_DATA COMMENT = 'Schema for APPLICATION_DATA related data';
Create or Replace Schema MASTER_DATA COMMENT      = 'Schema for MASTER_DATA related data';
Create or Replace Schema PEOPLE_DATA COMMENT      = 'Schema for PEOPLE_DATA related data';
Create or Replace Schema SUPPLY_CHAIN COMMENT     = 'Schema for SUPPLY_CHAIN related data';

USE DATABASE GOLD_DB;
Create or Replace Schema LEGACY_DW COMMENT        = 'Schema for LEGACY_DW related data';
Create or Replace Schema RETAIL_DATA COMMENT      = 'Schema for RETAIL_DATA related data';
Create or Replace Schema FINANCE_DATA COMMENT     = 'Schema for FINANCE_DATA related data';
Create or Replace Schema APPLICATION_DATA COMMENT = 'Schema for APPLICATION_DATA related data';
Create or Replace Schema MASTER_DATA COMMENT      = 'Schema for MASTER_DATA related data';
Create or Replace Schema PEOPLE_DATA COMMENT      = 'Schema for PEOPLE_DATA related data';
Create or Replace Schema SUPPLY_CHAIN COMMENT     = 'Schema for SUPPLY_CHAIN related data';
